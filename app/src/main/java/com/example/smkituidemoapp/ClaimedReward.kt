data class ClaimedReward(
    var rewardName: String = "",
    var rewardDescription: String = "",
    var dateClaimed: String = "",
    var id: String = "" // Document ID, which is the timestamp in this case
)
